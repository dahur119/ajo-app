{
  "title": "Business KPIs",
  "timezone": "browser",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "10s",
  "time": { "from": "now-24h", "to": "now" },
  "panels": [
    {
      "type": "text",
      "title": "Notes",
      "mode": "markdown",
      "gridPos": { "x": 0, "y": 0, "w": 24, "h": 4 },
      "options": {
        "content": "These panels expect service metrics to be instrumented. Implement counters like `users_registered_total`, `transactions_total`, `investment_created_total`, and a sum gauge/counter `transaction_amount_sum`. Once exported at `/metrics` and scraped by Prometheus, panels will populate."
      }
    },
    {
      "type": "stat",
      "title": "Signups Rate (users/s)",
      "gridPos": { "x": 0, "y": 4, "w": 8, "h": 6 },
      "targets": [
        { "refId": "A", "expr": "sum(rate(users_registered_total[5m]))" }
      ]
    },
    {
      "type": "stat",
      "title": "Transactions per Minute",
      "gridPos": { "x": 8, "y": 4, "w": 8, "h": 6 },
      "targets": [
        { "refId": "A", "expr": "sum(rate(transactions_total[1m]))" }
      ]
    },
    {
      "type": "stat",
      "title": "Average Transaction Value",
      "gridPos": { "x": 16, "y": 4, "w": 8, "h": 6 },
      "targets": [
        {
          "refId": "A",
          "expr": "(sum(rate(transaction_amount_sum[5m])))/(sum(rate(transactions_total[5m])))"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Investment Flows (events/s)",
      "gridPos": { "x": 0, "y": 10, "w": 24, "h": 8 },
      "targets": [
        { "refId": "A", "expr": "sum(rate(investment_created_total[5m]))" }
      ]
    }
  ]
}